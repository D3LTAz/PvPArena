# PvP Arena — Decisions Log

## 2026-09-07 — Milestone 1 definition
**Decision:** Milestone 1 is multiplayer deathmatch (1v1/2v2/3v3) on a single graybox map, matching the already-finalized Stage 1 scope — not the single-player graybox combat prototype named in the earlier Claude Code kickoff prompt.
**Reason:** Stage 1 scope (UE5 C++ template + Lyra reference, Git/Git LFS, one 3v3 graybox map, single starting weapon, server-authoritative hit registration with basic lag compensation, deathmatch across 1v1/2v2/3v3, minimal format-picker matchmaking) was already locked before the kickoff prompt was drafted, and the Game Director / QA Engineer specs are built around multiplayer from day one. Building single-player first means re-architecting for replication and server authority later — costs more than it saves.
**Who it affects:** game-director (owns the build order), qa-engineer (test plan should assume server-authoritative multiplayer from the start).

## 2026-09-07 — Studio Director stood up
**Decision:** Daniel's studio-director agent spec adopted as the standing point of contact for PvP Arena; began standing up the full specialist roster (game-director, qa-engineer, level-designer, technical-artist, social-media-manager, community-manager, sales-monetization, contracts-manager, finance-ops).
**Reason:** Requested directly.
**Who it affects:** all workstreams.

## 2026-09-08 — Git remote host
**Decision:** GitHub is the git remote host for PvP Arena (closes scaffold §3.3 open item).
**Reason:** Daniel's call.
**Who it affects:** game-director (scaffold doc's git workflow section), the repo/LFS setup steps.

## 2026-09-08 — Repo files staged on Daniel's machine
**Decision/status:** Wrote the full Phase-A-ready file tree to `~/source/repos/PvPArena` on Daniel's machine: `.gitattributes`/`.gitignore`, all 28 C++ class stub files (Source/PvPArena/Core, Character, Weapons, Matchmaking, UI) per §2 of the scaffold doc, `PvPArena.Build.cs`/`.h`/`.cpp` module skeleton, README with merge instructions for when UE5 generates the `.uproject`, all 10 planning docs, and the specialist agent roster (staged under `agents/`, not `.claude/agents/` — that path is blocked to remote file tools; Daniel needs to move it himself).
**Correction:** this session has no shell access to Daniel's machine (no device_bash tool available), only file transfer — so `git init`/`lfs install`/`add`/`commit`/`remote add`/`push` were not run. Daniel runs those himself; commands provided in chat.
**Who it affects:** game-director's Phase A (A3/A4 partially pre-done — files exist, git history does not yet).

## 2026-09-08 — New launch requirement: public demo build
**Status:** Daniel flagged a launch-gating requirement — a demo copy of the game must be published on the website before launch. Not yet scoped (platform, hosting, packaging target). Daniel is following up with a fuller goals list before this gets planned in detail.
**Who it affects:** game-director (packaging isn't in the current 40-task breakdown — new phase), and whichever agent ends up owning the website (no current agent owns web hosting/build distribution — flagged as a coverage gap).

## 2026-09-08 — Demo build scope (Milestone 2)
**Decision:** Daniel's demo requirements — title screen w/ Tutorial/1v1/1v1-AI/Settings menu, start-screen artwork, local LAN + single-player host, a tutorial map, a small "paintball ring" 1v1 map with barriers, AI opponents with difficulty settings, polished character/weapon/bullet visuals, first-to-3-kills as the standard mode — go beyond locked Milestone 1 (which is placeholder-art, internet matchmaking across 1v1/2v2/3v3, no AI, no menus, no tutorial). Studio-director recommendation: treat this as **Milestone 2 — Public Demo**, built on top of Milestone 1's engineering (replication, hit-reg, lag comp, health/weapon components carry over unchanged), not a replacement. Milestone 1 is not thrown away — it's still the fastest path to proving the hard netcode works before adding AI/art/menu polish on top.
**Reason:** Daniel's call to confirm; proceeding on this recommendation per standing instruction to keep moving rather than block.
**Who it affects:** game-director (new phases: menu/UI, LAN/local-host, AI opponents, win-condition tweak to 3-kill cap), level-designer (tutorial map + new small 1v1 map — distinct from the existing 3v3 Substation graybox), technical-artist (art polish pass — direct reversal of the "placeholder is fine" Stage 1 rule; likely needs contractor budget), finance-ops (cost estimate once art/AI scope is sized).

## 2026-09-08 — Two new roles: web-developer, discord-agent
**Decision:** Added `web-developer` (website, itch.io + `butler` demo-distribution pipeline, press kit page) and `discord-agent` (technical build-out of the Discord server — channels/roles/bots — implementing community-manager's policy) to the roster. This closes the "who owns the website" gap flagged when the public-demo requirement came in.
**Reason:** Requested directly. Scoped from research on how other indie developers actually ship demos and run outreach (itch.io/butler as the standard low-friction distribution path with built-in closed-testing access controls; Discord's own dev playbook + closed-beta playbooks for server structure; presskit.gg-style press kits) — see `docs/outreach-research-test-launch.md` for the full research and sourcing. Both roles are scoped to use existing standard tools (itch.io, butler, off-the-shelf Discord bots, templated press kit tools) rather than custom-built software by default; custom tooling is flagged as separate, priced scope if wanted.
**Who it affects:** web-developer takes the packaged build from game-director once Milestone 1/2 produce one; discord-agent implements the channel/role/bot structure community-manager already designed in `docs/community-structure-plan.md`, routing bug feedback into qa-engineer's existing format rather than a new one.

## 2026-09-08 — Title screen concept drafted
**Status:** First visual pass at the title/main-menu screen (Tutorial / 1v1 / 1v1 vs AI / Settings), published as a design canvas: https://claude.ai/code/artifact/fbc0269c-ffa0-47e0-9426-b362d7f5cdae
**Assumption made (no existing brand/logo to match):** dark, industrial, tactical HUD aesthetic — Teko display wordmark, single amber accent, corner-bracket "target lock" menu states, geometric barrier-plate motif echoing the Ring map — grounded in the already-established "tactical, grounded, CS/Valorant-style" identity rather than a new direction. Accent color is an editable tweak on the canvas, not locked in.
**Who it affects:** technical-artist (this is the concept direction for the real title-screen art pass in the Milestone 2 art-polish scope), game-director (menu layout/options match `UPvPMainMenuWidget`'s four options).

## 2026-09-09 — Free community asset research (maps, characters, weapons)
**Status:** Live-researched free/community assets to reduce or defer the contractor spend flagged in the art-polish scope. See `docs/free-community-assets-milestone2.md` for the full findings with sourced links.
**Key findings:** Manny/Quinn (built into UE5) + free Mixamo animations, retargeted via UE5's IK Retargeter, is the recommended zero-cost character base. No free weapon source hits the "polished" bar — a CC0 Sketchfab firearms kit or a confirmed-free Fab low-poly pack cover a placeholder weapon; the PBR/4K tactical rifles that match our visual bar are paid ($20–60 range). Quixel Megascans is **no longer free** via Fab (free-claim window closed Dec 31, 2024) — corrects an assumption from the original art-polish scoping; free CC0 texture alternatives (Poliigon free tier, TextureCan, 3dtextures.me) cover Substation's surfaces instead. No confirmed-free barrel/crate/barrier pack for The Ring surfaced — smallest and cheapest gap to just buy ($5–15) if nothing free turns up. UE5 Starter Content was removed starting engine 5.6/5.7 — don't plan VFX around it; Epic's free Niagara Examples Pack (UE 5.7) is the better current lead, including lightning effects that fit Substation's electrical theme.
**Reason:** Requested directly, to find free alternatives before committing contractor budget.
**Who it affects:** technical-artist (character/weapon/VFX sourcing), level-designer (Substation/Ring dressing), finance-ops (contractor budget likely narrows to: first-person arms rig, one signature weapon pass, Ring's cover props — not a full art contract), contracts-manager (note Fab's Standard License splits Personal/Professional at $100K/yr revenue — re-check asset licenses if token revenue approaches that line).

## 2026-09-09 — Free asset links live-verified (technical-artist)
**Status:** Ran technical-artist to live-verify the specific links flagged in the prior research pass, using a real rendered browser (automated WebFetch can't read Fab's client-side price fields, which is what left those links unconfirmed). Updated `docs/free-community-assets-milestone2.md` with results.
**Confirmed free:** Fab "Weapons FREE - Low Poly 3D Models Pack" (8 models, Unreal format included); Fab "Muzzle Flash (Niagara System)" (UE 5.4, drop-in); Fab "Weathered Blue Industrial Barrel - PBR 4K Texture" (game-ready poly count, closes part of The Ring's previously-flagged cover-prop gap).
**Confirmed paid:** Fab "Two concrete jersey barriers road traffic walls," $8.99–$11.99 — matches the prior flag.
**License correction:** the Sketchfab "Firearms_Kit_1.0" listing's title says CC0, but its actual License field reads CC-BY (Creative Commons Attribution) — commercial use is fine, but it legally requires crediting the author somewhere in the game. Also flagging its very high poly count (1.3M tris/669k verts) — needs decimation/retopo before it's game-ready, it's not a straight import. The Fab low-poly weapons pack is now the recommended lower-effort placeholder pick instead.
**Process correction:** the prior pass's "no confirmed-free barrel/crate/barrier pack for The Ring" finding was a false negative — Fab's search-result grid didn't render for the automated research tool, not an actual absence of free supply. Live browser search turned up several free barrel results immediately. Any remaining unconfirmed Fab link in the doc should get the same live-browser check before anyone budgets around its assumed price.
**Reason:** Requested directly ("run technical artist"), to verify before anyone imports assets into the project.
**Who it affects:** technical-artist (import order: barrel + weapons pack + muzzle flash are clear to pull in now; Sketchfab kit needs a retopo pass first if used), contracts-manager (add Sketchfab kit author credit to wherever third-party attributions are tracked — not yet a file that exists, flagging as a small gap), level-designer (The Ring can use the free barrel now; crates/hard-cover barriers still an open buy-or-keep-hunting call), finance-ops (no budget impact — net saves the $8.99–$11.99 barrier spend if the barrel alone is sufficient cover).

## 2026-09-09 — New character lead: Quantum Modular Character Free Sample
**Status:** Daniel found and I live-verified [Fab: "Quantum Modular Character Free Sample"](https://fab.com/s/f0475e248da1) — confirmed free (Standard License), UE 5.0–5.8, single UE5 skeleton, MetaHuman-compatible, tagged Military/Tactical/Soldier/Shooter/Rifle, includes bonus weapon static meshes. It's a free sample from Quantum Assets' paid modular bundle line ($99.99–$399.99 for the full kit) — one pre-built character, not the full module library.
**Decision:** Promoted this to the top character lead in `docs/free-community-assets-milestone2.md`, ahead of the Manny/Quinn + Mixamo recommendation, since it's a closer out-of-the-box match to our tactical-soldier identity at the same zero cost. Manny/Quinn stays the documented fallback if the sample's rig/texture quality disappoints once technical-artist checks it in-engine.
**Reason:** Requested directly (Daniel sourced the link).
**Who it affects:** technical-artist (pull this in and evaluate in-engine before committing to Manny/Quinn), contracts-manager (Standard License, same $100K/yr Personal/Professional tier note as other Fab assets applies).

## 2026-09-09 — New weapon lead: FPS Weapon Bundle (Deadghost Interactive)
**Status:** Daniel found and I live-verified [Fab: "FPS Weapon Bundle"](https://fab.com/s/4e9d5df313a2) — confirmed free, 7 weapons + 4 attachments, all rigged for animation, alternative textures included. Originally a 2018 Unreal Marketplace "Permanent Free Collection" sponsored pack, well-reviewed (4.6/5, 377 ratings).
**Decision:** Promoted to top weapon lead in `docs/free-community-assets-milestone2.md`, ahead of the low-poly weapons pack, since rigged + textured is real value the other free options don't have. Two open items before committing: listed UE compatibility is 4.12–4.27 (not explicit UE5 — needs an in-engine import check), and its license page reads "UE Marketplace" rather than Fab's current "Standard License" — flagged for contracts-manager to sanity-check the terms match.
**Reason:** Requested directly (Daniel sourced the link).
**Who it affects:** technical-artist (try this pack first; confirms UE5 import cleanly before relying on it), contracts-manager (verify "UE Marketplace" license terms).

## 2026-09-09 — Additional weapon options checked: ALowPolyCount rifle + SMG
**Status:** Live-verified [Fab: "Low Poly Assault Rifle 1"](https://fab.com/s/2b4e6e56a447) and [Fab: "Low Poly SMG 45"](https://fab.com/s/74d7fc92308d) (both ALowPolyCount) — free, Standard License, rigged single weapons with removable parts and camo-customizable textures. Blender/fbx/obj only, no native Unreal format.
**Decision:** Logged both as backup single-weapon options in `docs/free-community-assets-milestone2.md`, not promoted over the Deadghost FPS Weapon Bundle (which has 7 weapons, alt textures, and a much larger review base).
**Reason:** Requested directly (Daniel sourced the links).
**Who it affects:** technical-artist (fallback only, not the primary pick).

## 2026-09-09 — Rejected asset: CS2/CS:GO "Printstream" skin recreation (IP risk)
**Status:** Checked [Fab: "CS2 / CS:GO Printstream Collection"](https://fab.com/s/f7fbefae4de6) (KalashCG) — free, Standard License, Deagle/M4A1-S/USP-S models with Valve's "Printstream" finish applied.
**Decision:** Rejected — do not import. "Printstream" is Valve's specific, currently-sold copyrighted skin design for CS2/CS:GO, not a generic camo/finish. This listing is a fan recreation trading on the CS2/CS:GO name; Fab hosting it free doesn't grant clean usage rights, since the uploader doesn't own the design being replicated. Using a replica of a competitor's specific paid cosmetic in our own commercially monetized game (we have a token economy planned) is real infringement exposure. Same rejection applies to the creator's other listings in this series (Asiimov, Blaze, Cyrex collections) and to any other "[Real Game] skin recreation" asset if one surfaces.
**Reason:** IP/legal risk caught during live-verification, requested directly to check (Daniel sourced the link).
**Who it affects:** technical-artist (do not use), contracts-manager (worth a standing note: reject any found asset that recreates a specific named commercial skin/cosmetic from another live game, regardless of Fab's listed license).

## 2026-09-09 — Time-limited free find for Substation: Industrial Infrastructure
**Status:** Live-verified [Fab: "Industrial Infrastructure"](https://fab.com/s/80121e18b0e5) (Sierra Division) — normally $59.99, currently 100% off as a limited-time promo, **sale ends 2026-09-22 06:59**. UE 5.1–5.8, native Unreal format, 5.0★ (8 ratings). Strong Substation match: catwalks, pipes, beams, railings, ladders, vents, rusty metal panels.
**Decision:** Flagged as time-sensitive — recommended Daniel claim it (Add to Library) before the promo ends, since claimed items stay free permanently but the window closes 09/22. Added to `docs/free-community-assets-milestone2.md` Substation section.
**Reason:** Requested directly (Daniel sourced the link).
**Who it affects:** level-designer (Substation dressing), technical-artist (claim before the deadline), finance-ops (avoids a $59.99 spend if claimed in time).

## 2026-09-09 — Asset integration kickoff prompt drafted
**Status:** Wrote `docs/claude-code-prompt-asset-integration.md` — a narrow, content-only Claude Code prompt for wiring the confirmed-free assets (Quantum character, Deadghost weapon bundle, muzzle flash, barrel, Industrial Infrastructure kit) into the existing `UPvPWeaponData`/`UPvPWeaponComponent`/`APvPCharacter` architecture once Daniel finishes importing them via Fab.
**Reason:** Requested directly.
**Who it affects:** whoever runs Claude Code next on the local repo — explicitly scoped to art/content wiring only, no netcode changes, and points at the rejected-asset list so the Printstream skin (or its siblings) can't get pulled in by accident.

## 2026-09-10 — Practice vs AI: shared-code audit confirmed, local rebuild required
**Status:** A code audit (repo tip `7523847`, "FIRE BUILD 4") confirms 1v1 vs AI runs on the same code as the rest of the practice slice — not a leftover Blueprint pawn or a second fire path. Bot pawn is `APvPCharacter` (same class as the human), same `UPvPHealthComponent`, same `UPvPWeaponComponent` + weapon data assets, same `Fire()` entry point. Only difference is the controller: `APvPAIController` calls `Weapon->Fire()` from its own Tick (range/delay/cooldown) instead of the player's input-bound Tick check. `APvPDeathmatchGameMode` doesn't spawn a different pawn either. A stale comment in `PvPPracticeGameMode.h` referencing `BP_ThirdPersonCharacter`/`BP_ThirdPersonPlayerController` was wrong (no such Blueprint classes are in the live GameMode constructor) and is corrected.
**Decision:** No code path issue exists — this was purely a verification pass, no architecture change needed. However: **local PIE on Windows may still be running a stale pre-BUILD-4 Editor module** (hot-reloading a live Editor session is the known cause of previous sessions showing old fire/ammo behavior). game-director needs to do a clean local rebuild before trusting any further PIE testing: pull master (confirm `7523847`+), fully quit the Editor, regenerate/open in VS2022 (UE 5.8 pin — not VS2026), Clean + Rebuild `PvPArenaEditor` Win64 Development, relaunch from that binary, and confirm the "FIRE BUILD 4" fingerprint strings appear (tutorial line + HUD ammo counter) before debugging any input behavior. If those strings are missing after rebuild, the binary is stale, not a real second AI code path.
**Reason:** Verification pass to rule out a duplicate/parallel AI pawn before trusting practice-mode test results; cloud agent can't compile or PIE (no UE 5.8 on Linux), so this was a static-code audit only — the rebuild itself is a Windows+VS2022 job for game-director.
**Who it affects:** game-director (do the rebuild, confirm BUILD 4 fingerprints, then verify player ammo decrements on F/LMB and the bot still fires through the same `Fire()` path), qa-engineer (don't sign off on any Practice vs AI test pass until BUILD 4 strings are confirmed visible in PIE).

## 2026-09-10 — Reconciled with the real repo's history; sequencing set: practice-vs-AI ships first, multiplayer resumes later
**Status:** Discovered this cloud session had been syncing to a disconnected local folder (`source\repos\PvPArena`, never git-connected) while the actual GitHub-tracked repo (`Documents\GitHub\PvPArena`) had independently pivoted away from multiplayer to single-player practice-vs-AI on 2026-09-07, and shipped it (playtested, bug-fixed) by 2026-09-08 — all without this session's knowledge. The two histories diverged for three days.
**Decision:** Daniel's call: **practice-vs-AI ships first (already built and playtested); multiplayer deathmatch resumes as a later phase — both matter, neither is cancelled.** All of this session's Milestone 2 planning docs (`game-director-milestone2-demo.md`, `level-design-milestone2-demo-maps.md`, `claude-code-prompt-milestone2.md`) are now marked with a sequencing note as **Phase 3 / multiplayer-resume reference material**, not current build order, and pushed into the real repo alongside a matching reconciliation entry in its own `docs/decisions.md`. Still-current material (free-asset research, art-polish scope, outreach research, the asset-integration prompt, and the `web-developer`/`discord-agent` roster additions) was pushed there too, since none of it depends on which combat mode is current.
**Reason:** Requested directly, after flagging the divergence and getting Daniel's sequencing call.
**Who it affects:** Everyone — going forward, `Documents\GitHub\PvPArena` is the only real sync target; `source\repos\PvPArena` was scaffold/doc staging that never connected to git and is now dead weight (left alone, not deleted, since deleting wasn't asked for).

## 2026-09-10 — New high-fidelity weapon option checked: M4 Modular Kit (unrigged)
**Status:** Live-verified [Fab: "M4 Modular Kit"](https://fab.com/s/6b60e748a4c0) (Karnaval) — free, Standard License, 30–50k tris with per-part texture sets, real visual detail. File is `m4noskel.fbx` — **no skeleton, not rigged** — and the author flags known material issues (roughness/metal maps off as shipped, broken preview in their own tool).
**Decision:** Logged as a high-fidelity source mesh option in `docs/free-community-assets-milestone2.md`, not a drop-in replacement for the Deadghost bundle — it needs a rigging pass and a materials cleanup, whereas Deadghost is already rigged and usable as-is. Doesn't close the "first-person arms rig" gap already flagged in RISK/GAPS; if anything it sharpens why that gap matters (better source mesh, same missing rig).
**Reason:** Requested directly (Daniel sourced the link).
**Who it affects:** technical-artist (candidate for the "signature weapon" pass once rigging time is budgeted — not a placeholder-speed pick), contracts-manager (light note: creator requests credit, not contractually required under Standard License, but cheap to just do).

## 2026-09-10 — More free placeholder gun options checked
**Status:** Live-verified two more: [AR-15 style rifle](https://www.fab.com/listings/9d67f325-8817-4385-9871-02ad21b73a8c) (SpatialNeglect, free, Standard License, realistic/tactical, fictional logos to dodge trademark issues — no rig mentioned, likely static) and [FPS Guns 4K - Rifle 1](https://www.fab.com/listings/a7e70f32-2091-416c-8da9-1546b4dff1bb) (Heart State Games, free, 4K PBR, multi-piece but explicitly **not rigged/animated**, and its license blocks sharing/redistribution).
**Decision:** Added to `docs/free-community-assets-milestone2.md` as additional placeholder candidates. Neither is rigged — same pattern as the M4 Modular Kit. The two rigged, ready-to-use free picks already on file remain Deadghost's FPS Weapon Bundle and the ALowPolyCount rifle/SMG.
**Reason:** Requested directly.
**Who it affects:** technical-artist.
## 2026-09-07 — Engine pinned to UE 5.8; build toolchain is VS2022, not VS2026
**Decision:** `EngineAssociation` is `5.8` (per §0 of the scaffold, log the exact version at kickoff — done here). C++ project-file generation must target **Visual Studio 2022**, installed alongside whatever newer VS version is on a given machine — do not rely on the newest installed VS.
**Reason:** UE 5.8's `UnrealBuildTool` doesn't recognize Visual Studio 2026 (v18) as a supported project-file format. With only VS2026 installed, project generation failed with `Unexpected ProjectFileFormat 'Default'` (UBT detects the installed IDE, can't map it to a known format, and falls back to the literal unhandled string `"Default"`). Installing VS2022 side-by-side (Community edition, "Game development with C++" workload) resolved it.
**Who it affects:** game-director (build order assumes C++ compiles), anyone setting up the project on a new machine — check installed VS version before assuming project generation will "just work" on the newest VS available. `Config/DefaultEditorPerProjectUserSettings.ini` is now gitignored so a machine's resolved IDE/editor preference doesn't get committed and break other machines the way this did.

## 2026-09-07 — APvPDeathmatchGameMode drives its own match-state machine (AGameModeBase has none)
**Decision:** `APvPDeathmatchGameMode::SetMatchState()` is a small hand-rolled state setter that writes `EPvPMatchState` (`Source/PvPArena/Core/PvPMatchState.h`) onto `APvPDeathmatchGameState::CurrentMatchState`, rather than overriding `HandleMatchIsWaitingToStart`/`HandleMatchHasStarted`/`HandleMatchHasEnded`.
**Reason:** Those `Handle*` hooks only exist on the fuller `AGameMode`, not on `AGameModeBase` — which is the base class §2.1 of the scaffold explicitly calls for. Assumed otherwise while writing the B3 skeleton; the compiler caught it (`C2039: is not a member of 'AGameModeBase'`) before it shipped. Switching to `AGameMode` would have been the "easy" fix but silently pulls in its lobby/timeout machinery, which isn't a decision to make unilaterally.
**Who it affects:** Phase E (win condition / `EndMatch`) — call `SetMatchState(EPvPMatchState::InProgress)` / `SetMatchState(EPvPMatchState::Ending)` from there rather than expecting `AGameMode`-style virtuals to fire.

## 2026-09-07 — Pivot: single-player practice-vs-AI replaces multiplayer Stage 1 as the near-term target
**Decision:** Parking the multiplayer deathmatch/matchmaking/lag-compensation work (Phases D's rewind buffer, E's team logic, F's 3v3 map, G's matchmaking) in favor of a single-player "practice" build: one human player vs. one AI-controlled bot, in a small arena ("ring"), with 2-3 selectable weapons. This directly reverses the 2026-09-07 "Milestone 1 definition" decision above, which explicitly rejected this exact direction (single-player-vs-AI) in favor of multiplayer-first.
**Reason:** Requested directly by Daniel — wants a fun, demoable, playable slice sooner rather than finishing the full multiplayer stack first. The fixed stack rule (server-authoritative hit registration from day one — see `.claude/agents/game-director.md`) still holds: weapon fire is still server-validated hitscan, just without the historical rewind-buffer lag compensation, which exists specifically to compensate for *other players'* ping and has no real target here (the AI opponent is server-controlled directly, not a remote client). The GameState/PlayerState/GameMode/Character/HealthComponent framework from Phases A-C is reused as-is; none of that work is wasted if multiplayer deathmatch resumes later.
**Who it affects:** All remaining build order. Weapon system (was Phase D) now ships without lag compensation. New AI opponent and single-player game mode work replace Phases E-G for now. Level/arena work (was Phase F) shrinks from a 3v3 graybox to a small 1v1-vs-bot ring.

## 2026-09-08 — Practice-vs-AI milestone complete
**Decision:** Treating the single-player practice build (the 2026-09-07 pivot above) as done for now: tutorial/start overlay → match with a HUD (health/ammo/weapon/score) → first-to-3-kills win condition → victory/defeat screen → replay. Not resuming the original multiplayer Milestone 1 scope at this time.
**Reason:** Requested directly ("finish milestone one," clarified to mean the practice build, not the parked multiplayer scope). Playtesting surfaced and fixed several real bugs along the way worth noting for anyone touching this code next: `IMC_Default`/`IMC_MouseLook` had zero key mappings from project creation (silent — WASD/fire never worked, only raw-bound keys like F11/Q did); `HandleDeath`'s "respawn" was a log-only stub that permanently soft-locked the player on death; the AI bot could kill the player in ~1s by engaging at point-blank range with no warm-up; `FInputModeUIOnly` silently prevents `InputComponent::BindKey` from firing, so widget-dismiss keys must be handled in the widget itself (`NativeOnKeyDown`), not the controller; and `FirstPersonCamera`'s height used `BaseEyeHeight` as an offset from the capsule's *center* instead of its *base*, sitting the camera too low. All fixed and pushed (see commit history `a4a74c0`..`92af473`).
**Who it affects:** Anyone resuming multiplayer work later — the GameState/PlayerState/GameMode/Character/HealthComponent/WeaponComponent framework is unchanged and reusable; only APvPPracticeGameMode, the AI controller, the tutorial/HUD/result widgets, and Lvl_PracticeRing are practice-mode-specific and would need swapping out (or kept as a permanent training mode alongside real PvP).

## 2026-09-08 — Any non-interactive UMG overlay must set Visibility(HitTestInvisible)
**Decision:** `UPvPHUDWidget`'s root `SOverlay` explicitly sets `.Visibility(EVisibility::HitTestInvisible)`. Applies to any future pure-display widget added to the viewport during active gameplay (as opposed to the tutorial/result screens, which are meant to capture input).
**Reason:** The player could not fire at all after the HUD shipped — confirmed via log that only the AI bot (which calls `Fire()` directly in C++, bypassing input) ever fired; the player's identically-bound Left Click never reached `HandleFireInput`. Root cause: Slate widgets default to `EVisibility::Visible`, which is hit-testable. A full-screen `SOverlay` with that default silently swallows every mouse click before it reaches Enhanced Input, even though it contains nothing but display text and was never meant to be interactive. Keyboard input was unaffected (keyboard routing is focus-based, not hit-test/position-based), which is why WASD/Q/V kept working while only Left Click silently died — a useful diagnostic signature if this class of bug recurs.
**Who it affects:** Any future HUD/overlay work shown during live gameplay (ammo counters, kill feed, minimap, etc.) — default to `HitTestInvisible` unless the widget genuinely needs to receive clicks.

## 2026-09-08 — Milestone 2 scope adapted from an out-of-sync kickoff prompt
**Decision:** A "Milestone 2 kickoff prompt" was handed over assuming Milestone 1 = a fully multiplayer deathmatch build (real matchmaking, lag compensation, PvP between human players) already exists in this repo, and referencing docs (`docs/game-director-milestone2-demo.md`, `docs/level-design-milestone2-demo-maps.md`, `docs/technical-artist-milestone2-art-polish.md`) that don't exist here. Neither premise holds for this repo — what exists is the single-player practice-vs-AI build from the 2026-09-07 pivot. Rather than block on sourcing those files, adapting the prompt's *intent* to what's actually built: menu shell, data-driven AI difficulty, settings, tutorial — dropping/reshaping the pieces that assume a multiplayer foundation that isn't there.
**Reason:** Requested directly, after flagging the mismatch and getting explicit direction to adapt rather than wait for the missing docs or the assumed multiplayer base.
**Who it affects:** Everything from here forward in this pass. Concretely: no LAN/matchmaking wiring (nothing to wire to); `APvPAIController` keeps its existing Tick-based approach rather than a `UAIPerceptionComponent` + Behavior Tree rewrite (its tunables move into a new `UPvPAIDifficultyProfile` data asset instead, which is the actual point of that constraint — data-driven difficulty, not a specific implementation technique); the tutorial stays the existing pre-match overlay rather than a separate `APvPTutorialGameMode`; win condition needs no change (already at a 3-kill cap). Settings and the menu shell are built as specified since they don't depend on the missing multiplayer foundation.

## 2026-09-08 — QA pass (code inspection) caught 4 real P0s in the Milestone 2 wiring
**Decision:** Fixed all four without argument — a from-source QA report (no live PIE session available to the reviewer) correctly traced each to the actual cause: (1) the bot's `TargetAcquiredTimeSeconds` was only ever set once at spawn, so its engagement delay had nothing to reset against on player respawn and it could fire immediately; (2) the level had exactly one `PlayerStart`, so player and bot respawn could land on top of each other; (3) `UPvPWeaponComponent::Fire()` never decremented `CurrentAmmo`, so the HUD ammo readout and "empty weapon" state were fictional; (4) "Play Again" reloaded the level, which — since `BeginPlay` always shows the main menu — meant it silently returned to the menu instead of starting a rematch.
**Reason:** All four block calling the slice actually playable end-to-end, which was the explicit bar this QA pass was testing against.
**Who it affects:** `APvPCharacter::HandleRespawn()` is now public and safe to call on a living character, not just a corpse — anything building on top of it (e.g. a future real respawn-selection system) should preserve that. Two `PlayerStart`s now exist in `Lvl_PracticeRing`, tagged `PlayerSpawn`/`BotSpawn` via `APlayerStart::PlayerStartTag` — any future level for this mode needs the same two tagged starts, or spawn selection silently falls back to "any start found first."

## 2026-09-08 — QA pass 2 (code inspection only) caught 2 more real bugs in HandleRespawn/AI fire

**Decision:** Fixed both. (1) `HandleRespawn()` never cleared `RespawnTimerHandle` — `RestartMatch()`'s in-place rematch calls `HandleRespawn()` directly, but if a death happened just before "Play Again," the original death-triggered timer was still pending and fired again a few seconds into the new match, re-teleporting/re-arming everyone a second time. Now `HandleRespawn()` clears that timer as its first action. (2) `HandleRespawn()` reset health but never ammo, so dying or rematching with a partially/fully empty clip carried the empty clip into the next life — now it re-calls `WeaponComponent->EquipWeapon(WeaponComponent->WeaponData)`, which refills `CurrentAmmo` to max as a side effect of re-equipping the same weapon. Also fixed `APvPAIController::Tick()`: the bot had no reload and shared the same ammo gate as the player, but nothing ever pressed "Q" for it, so an emptied bot would stand at its target attempting to fire nothing for the rest of the match — it now calls `Weapon->CycleWeapon()` instead of `Weapon->Fire()` whenever its current weapon is out of ammo.
**Reason:** A second from-source QA pass (still no PIE access on the reviewing side) traced both from the existing respawn/fire code paths; both were confirmed by direct inspection here before fixing. The QA pass's third hypothesis — that `UPvPHUDWidget` itself (the `UUserWidget`/`SObjectWidget` wrapper, not just its root `SOverlay`) needed `SetVisibility(HitTestInvisible)` to stop swallowing the player's Left Click — was NOT applied: `HandleDismissTutorial`/`HandlePlayAgain` both call `SetInputMode(FInputModeGameOnly())` before the HUD is even created, and `FInputModeGameOnly` pins Slate focus to the viewport widget itself, bypassing UI hit-testing for input routing entirely — so the HUD's own hit-test visibility shouldn't be able to block gameplay input regardless of its value. Diagnostic logging (commit `9494032`, still in place) will confirm directly on the next PIE session whether Enhanced Input ever calls `HandleFireInput` at all; changing widget visibility speculatively without that evidence risks masking the real cause.
**Who it affects:** Anyone touching `HandleRespawn()` — it's now the single place that resets health, ammo, and pending timers together for both player and bot, on death and on rematch; don't reset those individually elsewhere. AI fire logic assumes `CycleWeapon()` is always safe to call mid-fight (it is — see `EquipWeapon`).

## 2026-09-10 — Reconciled with the cloud-session planning docs; sequencing set: practice-vs-AI ships first, multiplayer resumes later
**Status:** A parallel cloud session (studio-director role, same spec as this repo's) had spent 2026-09-07 through 2026-09-09 planning a "Milestone 2" on top of an assumed-multiplayer Milestone 1, without visibility into this repo's actual history — specifically the 2026-09-07 pivot to single-player practice-vs-AI (above) and everything built since. The two diverged. Reconciled today.
**Decision:** Daniel's call: **practice-vs-AI ships first (it's already built and playtested); multiplayer deathmatch resumes as a later phase — both matter, neither is cancelled.** Concretely: `docs/game-director-milestone2-demo.md`, `docs/level-design-milestone2-demo-maps.md`, and `docs/claude-code-prompt-milestone2.md` (added to this repo today, pushed from the cloud session) are marked as **Phase 3 / multiplayer-resume reference material** — not current build order — with a sequencing note prepended to each. They assume LAN/matchmaking exists; it doesn't yet. `docs/game-director-stage1-scaffold.md` (already in this repo) remains the correct multiplayer architecture reference for when that phase resumes — nothing in it was invalidated by the pivot.
**Also brought over from the cloud session, not phase-locked (useful now):** `docs/free-community-assets-milestone2.md` (live-verified free weapon/character/VFX/prop assets — see its own file for what's confirmed free vs. rejected for IP reasons), `docs/technical-artist-milestone2-art-polish.md` (art scoping), `docs/outreach-research-test-launch.md` (website/itch.io/Discord distribution research), `docs/claude-code-prompt-asset-integration.md` (a prompt for wiring the free assets into whichever character/weapon classes are current — written against the Stage 1 class names, which are confirmed still valid post-pivot). Two new roster agents also added: `web-developer`, `discord-agent` (see `.claude/agents/`) — `studio-director.md` updated to list them.
**Reason:** Requested directly, after flagging the divergence and asking Daniel how to sequence it.
**Who it affects:** game-director (own both phases — practice-vs-AI polish/content now, multiplayer resume later; don't start LAN/matchmaking work without a fresh go-ahead), technical-artist (free-asset doc and art-polish scope apply to the practice-vs-AI build now, not gated on multiplayer), level-designer ("The Ring"/"Onboarding Range" specs need a check against what practice-vs-AI's `Lvl_PracticeRing` already has before building anything from them), everyone else on the roster (web-developer/discord-agent work — demo distribution, community setup — isn't gated on which combat mode ships, proceed independent of this sequencing question).
**Correction (same day):** This entry was lost from git once already — it existed only as an uncommitted local edit and got wiped by a `git reset --hard origin/master` run during the fire-input-bug merge below, before being reconstructed from conversation history and committed here. The reference docs it names (`docs/game-director-milestone2-demo.md` etc.) are still genuinely present on disk but remain **uncommitted/untracked** as of this writing — worth committing deliberately rather than assuming they're already safe in git.

## 2026-09-10 — Fire-input bug resolved; root cause never fully confirmed
**Decision:** The player-can't-fire bug (reported repeatedly since the practice-vs-AI milestone shipped) is fixed. `APvPCharacter`/`APvPPlayerController` now trigger `UPvPWeaponComponent::Fire()` off the legacy raw-key path (`BindKey` for LMB/F on the Character, plus continuous `PlayerTick` polling of `IsInputKeyDown`/Slate's pressed-mouse-buttons on the Controller) instead of relying on Enhanced Input's `IA_Fire` action. `UPvPWeaponComponent::Fire()` also now calls `PerformServerTrace` directly when the owner `HasAuthority()`, rather than always going through the `ServerFire` RPC, since a Server RPC on a listen-server-hosted pawn with no remote connection was suspected of being silently dropped.
**Reason:** Extensive live-PIE diagnosis (logging every Enhanced Input trigger state, comparing `IA_Fire` against known-working `IA_Jump`/`IA_Move`, testing alternate keys, checking for duplicate assets, checking `bShouldFlushPressedKeysOnViewportFocusLost`) proved conclusively that **no action bound to a mouse button ever reached Enhanced Input's trigger evaluation in this project/environment** — `IA_Jump` mapped to LeftMouseButton (alongside its already-working SpaceBar mapping) never fired either. Multiple plausible-sounding fixes along the way (`IA_Fire` missing a Trigger; the focus-flush setting) were real, correct fixes for real problems they identified, but neither actually resolved the underlying issue, and the true mechanism was never pinned down. A parallel session (Cursor, the QA/bug-finder tool set up earlier) reached a working fix independently by bypassing Enhanced Input for Fire entirely — pragmatic given how thoroughly the EI path was proven broken here, even without a root cause.
**Who it affects:** Anyone adding new mouse-button-driven actions in this project should not assume Enhanced Input will deliver them — verify in a live PIE session before relying on it, and prefer the raw `BindKey`/`IsInputKeyDown` pattern now used for Fire if it doesn't. `IA_Fire`'s Enhanced Input binding (`HandleFireInput`, bound to `ETriggerEvent::Triggered`) and its `IMC_Default` mapping are left in place as a harmless redundant path in case Enhanced Input's mouse-button handling ever starts working in some environment — `Fire()`'s own cooldown gate makes a duplicate call from both paths in the same frame a no-op, not a double-fire. Diagnostic-only code from the investigation (Enhanced Input per-trigger-state logging, `HasMappingContext` logging, "FIRE BUILD" debug banners in the tutorial/HUD/on-screen text) has been removed now that the fix is confirmed live in PIE (player fires all three weapons, ammo decrements, hits register).

## 2026-09-10 — Wired the cosmetic-asset hooks ahead of actually importing anything from Fab
**Decision:** `UPvPWeaponData::WeaponMesh`/`MuzzleFlashFX`/`FireSound` already existed as fields but nothing read them anywhere. Wired them up so assigning them on a weapon data asset is the *only* step needed once real art exists — no further code changes: `APvPCharacter` now has a second, hidden `WeaponSkeletalMeshComponent` alongside the existing placeholder `WeaponMeshComponent`; `HandleWeaponEquipped()` prefers `WeaponData->WeaponMesh` (loading it onto the skeletal component and hiding the placeholder) and only falls back to the placeholder cube if no real mesh is set. Added `GetActiveWeaponMeshComponent()` so other systems can find whichever one is currently showing. Added `UPvPWeaponData::MuzzleSocketName` (defaults to `NAME_None`) and wired `UPvPWeaponComponent::MulticastConfirmedHit_Implementation` to spawn `MuzzleFlashFX` attached to that socket (or unattached at the trace origin if the socket name is unset/doesn't exist on the mesh) and play `FireSound` at the same point, every shot. Also extended `.gitattributes` LFS tracking for formats the free-asset research doc's sources actually ship in but weren't covered yet: `.obj`, `.glb`, `.gltf`, `.jpg`/`.jpeg`, `.hdr`, `.flac`, `.uexp`, `.ubulk`.
**Reason:** Requested directly ("continue with the build and prepare it for assets found through fab"). No Fab assets have actually been imported into `Content/` yet as of this entry (confirmed by search — no Deadghost/Quantum/Sierra Division/etc. folders exist), and importing them requires the Fab website/Epic Games Launcher + "Add to Library"/"Add to Project," which isn't something achievable from this session — that step is still on Daniel (or whoever pulls the assets in), following `docs/claude-code-prompt-asset-integration.md`'s existing instructions. What *is* achievable ahead of time is making sure the moment those assets land and get assigned to a `UPvPWeaponData` instance (or `APvPCharacter`'s hardcoded mesh/anim `ConstructorHelpers` paths, which still need a manual C++ edit + rebuild since the class is intentionally concrete, not Blueprint-subclassed — see the class comment), the game actually uses them without another round of plumbing work.
**Who it affects:** Whoever runs the asset-integration prompt next — the weapon mesh/VFX/sound wiring described in its "work order" step 2/3 is already done; that prompt's actual remaining work is importing the assets and assigning the references (`WeaponMesh`, `MuzzleFlashFX`, `MuzzleSocketName`, `FireSound`) on the `DA_Weapon_*` data assets, plus the character mesh swap (step 1), which still needs the `ConstructorHelpers::FObjectFinder` paths in `APvPCharacter`'s constructor hand-edited to point at whichever mesh/anim-BP the Quantum sample (or Manny/Quinn fallback) actually resolves to, since that's a compile-time reference, not data-driven. Verified headlessly that all three existing `DA_Weapon_*` assets still load cleanly with the new `MuzzleSocketName` field defaulting to `None`.

## 2026-09-09 — Sierra Division pack recovered from a misdirected Fab import; two disposable scaffold projects cleaned up
**Decision:** Daniel pulled the Sierra Division "Industrial Infrastructure" pack (the time-limited free promo from `docs/free-community-assets-milestone2.md`, expires 2026-09-22) via Fab, but it landed in a brand-new default-template project Fab created alongside this repo (`PvPArena/PvPArena/`, nested) instead of this actual game project. Copied `Content/SD_Art/` (301 assets, 3.9GB) directly into this project's `Content/` at the same relative path — pure content with no C++ dependencies, so this works cleanly since package paths (`/Game/SD_Art/...`) resolve identically regardless of which project the files sit in. Verified headlessly: all 301 assets (meshes, materials, textures, the demo map) load with no broken references. Committed and pushed via Git LFS. Both now-redundant scaffold projects (`PvPArena/PvPArena/`, 8.7GB, and a second empty attempt `PvPArena/PvPArena1/`, 4.6GB of pure build-intermediate cache with zero actual content) were deleted after their content was confirmed migrated.
**Reason:** Requested directly. The `Industrial_Infrastructure.umap` inside the pack is Sierra Division's own showcase level, not something we ship as-is — the useful part is the individual modular pieces (`SM_Beam_*`, `SM_Ladder_*`, `SM_Floor_*`, materials) for building actual level geometry into `Lvl_PracticeRing` or a future Substation map.
**Also this session:** A second Fab download, cached locally at `PREY_Shotgun-6ab53216` (Epic Games Launcher Vault Cache, raw FBX + textures, never imported into any project), was rejected before import. Its own listing metadata describes it as a fan recreation of a specific weapon from Arkane/Bethesda's *Prey* (2017) — "I loved the 2017 Prey game... I want to do something from the game so here it is." This is the same class of IP risk already flagged for the CS2/CS:GO "Printstream" skin in `docs/claude-code-prompt-asset-integration.md` §2: a free download doesn't clear the underlying risk of replicating another company's specific, recognizable commercial-game asset. Deleted from the local Fab cache at Daniel's confirmation; never touched this repo.
**Who it affects:** Anyone pulling more assets from `docs/free-community-assets-milestone2.md` (or finding new ones) — always open the target project in the editor *before* using Fab's "Add to Project," or double-check which project Fab actually targeted, to avoid a repeat of the misdirected-import cleanup this required. Also: apply the same "is this actually a replica of a specific commercial game's named asset, not a generic design" check used here to any future weapon/character find, free or not — check the listing's own title/tags/description, not just its license field.

## 2026-09-10 — decisions.md merge note
**Status:** The working copy of this file was overwritten locally by the other cloud/studio-director session's own reconciliation pass, based on a stale read that predated several entries added here since (the VS2022 toolchain pin, the AGameModeBase finding, the pivot decision itself, both QA passes, the fire-input resolution, the Sierra Division/PREY entries). Both sides' content was legitimate, so this session's missing entries were appended back above rather than either side being discarded. No content lost; entries above this note may be slightly out of strict chronological order as a result of the merge.
**Who it affects:** Anyone editing this file from either session going forward — re-read the file immediately before appending, since it's now confirmed under concurrent edit from more than one session on this same machine/repo.

## 2026-09-10 — Quantum Modular Character wired into APvPCharacter (mesh only, no animation yet)
**Decision:** `Content/QuantumCharacter/` (the Fab pack, same misdirected-import-then-recovered pattern as the Sierra Division pack — landed in a fresh default-template scaffold project instead of this one, copied over directly, verified headlessly, committed via LFS) is now `APvPCharacter`'s mesh, replacing Manny. Its bind-pose bounds are within ~0.1 units of Manny's in every dimension (verified headlessly via `USkeletalMesh::get_bounds()` on both), so Manny's already-tuned relative transform (`-96` Z offset, `-90°` yaw) carries over directly rather than needing to be re-derived. No `AnimInstanceClass` is set — `SK_Military_Character_Skeleton` isn't Manny's skeleton, so the existing `ABP_Unarmed` won't bind to it, and building a proper AnimBP (blend/state-machine for the pack's native Idle/Walk/Run/Jump/Fall/Land clips) turned out not to be scriptable at all: the stable Python editor API (`BlueprintEditorLibrary`, `EdGraph`, `EdGraphNode`) exposes no way to create or wire Blueprint/AnimGraph nodes — no `add_node`, no `EdGraphPin` class, no node-spawner utility of any kind. The classes (`AnimGraphNode_StateMachine` etc.) exist as reflected types but can't be connected into a working graph through this API. Confirmed via direct probing before attempting, not assumed.
**Reason:** Requested directly ("proceed" after being offered the choice between scripting the AnimBP blind, swapping the mesh now with animation deferred, or holding off entirely — picked the scripted-AnimBP option; that option turned out to be a hard technical wall rather than just a risk, so fell back to the mesh-now/animation-later path once that was established).
**Who it affects:** Whoever builds the actual AnimBP next (in-editor, not scriptable) — needs a new `ABP_QuantumCharacter` (or similar) targeting `SK_Military_Character_Skeleton`, using `Content/QuantumCharacter/Demo/Animations/A_MM_*` (Idle/Walk_Fwd/Run_Fwd/Jump/Fall_Loop/Land), then assign it to `APvPCharacter`'s mesh component's `AnimInstanceClass`. Until then, the character renders in its static reference pose in PIE — expected, not a bug. Also worth noting: two `.uproject`/`decisions.md` files went missing from the working tree during this session's headless script runs (both fully recoverable via `git checkout`, since both were tracked-but-uncommitted at the time) — cause not conclusively identified, flagged in case it recurs.
